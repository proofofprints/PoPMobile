# Drop screenshot PNGs here. Referenced from the root README.
#
# Expected filenames:
#   dashboard.png   — main screen while mining
#   settings.png    — settings panel (miner configuration + PoPManager)
#   logs.png        — mining log view
#
# Keep images under ~600 KB each; crop to phone viewport (no device chrome).
